const numero = 2; // se puede cambiar este número
let multiplicador = 1,
  Resultado;

while (multiplicador <= 10) {
  Resultado = multiplicador * numero;
  console.log("Resultado: " + numero + "*" + multiplicador + " = " + Resultado);
  multiplicador++;
}
