// Declara calificacion (0-100). Usa if-else if para
// mostrar: "Aprobado" (>=60), "Reprobado" (<60).

let clasi = 70;

if (clasi >= 60) {
  console.log("aprobado");
} else if (clasi < 60) {
  console.log("reporado");
}
